// 组件类型
export enum ComponentType {
  primary = 'primary',
  secondary = 'secondary',
  success = 'success',
  danger = 'danger',
  warning = 'warning',
  info = 'info',
  light = 'light',
  dark = 'dark',
  empty = 'empty',
}

// 数据类型
export enum DataType {
  number = 'number',
  boolean = 'boolean',
  string = 'string',
  array = 'array',
  json = 'json',
}

// 大小类型
export enum SizeType {
  sm = 'sm',
  lg = 'lg',
}
